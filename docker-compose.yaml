version: "3.9"
services:
  app:
    image: node:22-alpine
    container_name: delivery-os-hub
    restart: always
    working_dir: /app
    command: node server/index.mjs
    ports:
      - "3000:3000"
    volumes:
      - ./output:/app:ro
    environment:
      - NODE_ENV=production
      - PORT=3000
      - HOST=0.0.0.0
    networks:
      - app-network

networks:
  app-network:
    driver: bridge
